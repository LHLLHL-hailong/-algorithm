LK02: 单独设置一个PTR,用于指示当前正在生成节点的前一个节点的指针
LK03: 简单的存在不存在使用数组或者是HashSet就可以不用设置diff
LK11: 每次只移动短的
LK31: 从后到前，找第一个递减的数字置换，并在后边进行重排序，没有则全量重排序（这里的排序就是左右镜像）
LK33: 先解决target 和nums[mid] 不同时大于/小于 kvalue，剩下的情况就是正常的二分查找的过程
LK48: 顺时针90°=上下+对角线 / 对角线+左右
LK49: char数组可以进行排序
LK75: 同边mid++,异边不能变动
LK78: 设置一个startIdx，保证它的组合逻辑，但是要注意单独控制什么都不选的逻辑
LK105: 对中序遍历设置一个映射map，方便查询节点
LK128: 只从连续序列的起点出发，用判前驱代替删元素，避免遍历时修改集合。(防止并发修改异常)
LK139: 使用HashSet存储每一个部分的单词片段
LK142: 简单的一个双指针判断是是否存在环和入口节点的位置
LK146: 双向链表+HashMap
LK148: 快慢指针用于分割，实现归并排序（标准的快慢指针，用慢指针的下一个用作第二个链表的开头）
LK152: 可以使用两个变量状态压缩两个dp数组，不过需要注意这个存在两个状态转移公式的状态压缩之后状态转移方程相互污染的情况（状态转移的时候别忘了还有从nums[i]开始放弃之前的所有的时候）
LK155: 需要使用long类型
LK200: 更改原数组而不是建立新的isVisited
LK207: 标准的拓扑排序
LK208: 需要维护end，但是不需要维护pass
LK215: 用nums.length-k将找第k大的数字变成从小到大进行排序后下标为nums.length-k的，进行快速排序+二分查找的是否就不用考虑k的变化了（因为下标不变）（函数里面要按照范围遍历，遍历变化时不能遍历到右边界，方式误移piviot）(这个题目的快速排序阶段必须对等值做出优化，否则会超时)
LK221: 三个正方形状态转移成为一个正方形
LK240: 从矩阵的右上角开始，大于他就下移，小于它就左移
LK283: 别忘了，前面成功赋值了，但是后边后续别忘了归零(必须这么做,必须用覆盖而不是swap尾指针,否则答案数组中的元素的顺序不对)
LK309: 区分两个状态
LK322: 这个本质上是一个完全背包，二维的话别忘了容积没到达coins[i-1]的时候也需要进行状态转移
LK337: 数的后续遍历，dfs返回的是偷/不偷这个节点产生的最大收益，在后序遍历中动态规划（需要注意这个节点不偷可以用子节点 偷/不偷 进行状态转移，是两个的最大值）
LK338: 用循环+&+有移来判断1的数量

##need
1. LK31
2. LK33
3. LK48
4. LK139
5. LK155
6. LK215