LK02: 单独设置一个PTR,用于指示当前正在生成节点的前一个节点的指针
LK03: 简单的存在不存在使用数组或者是HashSet就可以不用设置diff
LK11: 每次只移动短的
LK31: 从后到前，找第一个递减的数字置换，并在后边进行重排序，没有则全量重排序（从排序使用左右镜像的方式可以推导出成立）
LK33: 先保证，target 和nums[mid] 同时大于/小于 kvalue
LK48: 顺时针90°=上下+对角线 / 对角线+左右
LK49: char数组可以进行排序
LK75: 同边mid++,异边不能变动
LK78: 设置一个startIdx，保证它的组合逻辑，但是要注意单独设置控制
LK105: 对中序遍历设置一个映射map，方便查询节点
LK128: 只从连续序列的起点出发，用判前驱代替删元素，避免遍历时修改集合。(防止并发修改异常)




##need
1. LK31
2. LK33
3. LK48